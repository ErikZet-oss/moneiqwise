import { AddTransactionForm } from "@/components/AddTransactionForm";

/** Zachované kvôli starým odkazom; presmerovanie je v App.tsx na /history. */
export default function Transactions() {
  return <AddTransactionForm />;
}
